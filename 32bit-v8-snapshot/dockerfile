# Start from the smallest docker image with shell (~5mb)
FROM alpine

MAINTAINER NativeScript

# HINT:
# Do not cache dependencies in order to avoid a bigger size. For example:
# RUN apk add --no-cache wget

# Add the required snapshot tools dependencis
# The dependencies are generated by following http://glaudiston.blogspot.com/2015/06/how-to-make-very-very-small-docker.html
COPY ./lib/ /lib/
